# Sarah — her common library

The full guide — what each of the 7 folders holds, the rest-pose and
uncertainty standards, why idle footage is rendered rather than mined, how
the Avatar Editor works with this folder alongside a store's own
`sarah_clips/`, and the traps already paid for — moved to
**`.claude/skills/sarah-library/SKILL.md`** (2026-09-03), so it's read
before working with any of this rather than only found by browsing here.

This file stays as the folder's own landing note. Two rules that don't
belong in the skill, because they're about THIS folder specifically, not
about Sarah generally:

- Keep one file per standard, named for the moment it covers, and record
  where it came from — a clip + timestamp for a scraped frame, or the
  request that generated it.
- Everything here is **tracked** in git — unlike the video files under
  `Customers/*/*/help-videos/`, which are gitignored.
