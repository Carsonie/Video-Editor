# ski-demo — add-collection (BCP admin)

**Purpose: VIDEO SEED** — raw footage for a help/promotional video, to be finished in the Video-Editor repo. Not a bug repro.

- **File:** ski-demo_add-collection_dev_09-01-19_v1.mp4
- **Recorded:** 2026-09-09, localhost (mandatory for video seeds)
- **Target:** dev (http://localhost:8080), store `ski-demo` / `store_d2offcn2rd0c73asp090`, business `Rentify Demo Corp`
- **Surface:** BCP admin (`bcp_runner.ts`), sequence `add-collection` (2 steps: create_collections then create_items), recipe slice `video_add-collection.Store.yaml`
- **Result:** flow PASSED. Created collection "Mens Ski Poles Carbon" (collection_dagen7v2djacbd72tpag), then 2 items in it: "Mens Ski Poles Carbon 115 cm" and "Mens Ski Poles Carbon 120 cm"
- **Duration:** ~230.8s, 2304x1926 @ 25fps

## What's on screen
1. Login screen (2s hold), email verification / auth code entry
2. Click-through nav to Lookup Collections, "+" -> New Collection form: name, description, price, options (JSON) typed in, saved
3. Click-through nav back to Lookup Items (via 6x back to dashboard), "+" twice -> two New Item forms, each: name typed, collection searched/selected ("Mens Ski Poles Carbon"), requirement selected, variant (Ski Pole Length = 115cm / 120cm) selected, saved
4. Logout, 2s hold on login screen

**One continuous browser window for the whole sequence** — confirmed by the run log printing "browser: one window for all 2 step(s)". The handover between step 1 (create_collections) and step 2 (create_items) is a no-op internally (step 1's logout logs "staying signed in", step 2's login logs "already signed in — reusing this session") — there is exactly ONE real login (9.7s, start) and ONE real logout (3.7s, end), matching the documented fix. Verified no desktop bleed at the internal step seam (t≈109-111s) and end of video (t≈229s, back on login screen) by frame extraction — clean throughout.

## Post-run cleanup (done)
`create_collections` wrote the real collection id back into `video_add-collection.Store.yaml`. Per the file's own comment, that `id:` line was deleted after this take so the file is re-runnable for future takes. No other lines touched.

## Known issues
None. No desktop bleed anywhere in the capture, including the step-1/step-2 handover inside the sequence.
