# add-requirement — ski-demo

**Purpose:** Video seed (not bug repro) — raw footage for a help/promo video,
finished in the Video-Editor repo.

**Standard:** 2026-09-09 raw-video standard (yellow-ring-only highlight held
through typing, per-screen narration lines already written into the vtt/script
beside this file, SCREEN_MIN_MS enforced per screen).

**Command used:**
```
cd Master_Flows/Recorder
CLICK_HIGHLIGHT_FILL=0 TYPE_HIGHLIGHT=1 \
SCREEN_MIN_MS="6200,6200,5700,4800,5100,3300,4800,4800,5400,7400,4500,4200" \
RECIPE=add-requirement npx tsx scripts/record_flow.ts --surface bcp ski-demo add-requirement
```

**Target:** localhost (dev), surface: bcp.

**Result:** ski-demo_add-requirement_dev_09-09-29_v2.mp4 — 279,939,853 bytes
(267.0 MB), 93.16s. Flow passed. Expected window ~80-90s; this ran ~93.2s,
about 3s over — same launch-overhead margin as add-question, not a defect.

**Deep links:** grep -c "deep link" = 0, checked against the Recorder's own
full-output log (Master_Flows/Recorder/_logs/ski-demo/09-09_02-record-add-requirement.log).
Note: create_requirement.test.ts, like create_questions.test.ts, does not write a
testing/log_reports/ entry at all — it never calls logReportDirForStoreFile (only
create_items/create_collections do). No new log_reports file was produced by this
run. This is a gap in the flow itself, not something fixed here — flagged for
whoever owns that flow.

**bcp_runner browser:** "one window for all 1 step(s)" — single browser, no desktop seam.

**Screen table** (from the terminal's "⏱ screen N" lines — not present in saved log files):

| # | screen | on for | note |
|---|---|---|---|
| 1 | the sign-in screen | 5.9s | held 0.3s more → 6.2s |
| 2 | the code screen | 3.1s | held 3.1s more → 6.2s |
| 3 | dashboard | 1.1s | held 4.6s more → 5.7s |
| 4 | lookup, before picking "Rentify Demo Corp" | 5.4s | over its 4.8s minimum |
| 5 | the page, before Lookup Stores | 2.1s | held 3.0s more → 5.1s |
| 6 | lookup, before picking "Ski Demo - Blue Mountain" | 6.6s | over its 3.3s minimum |
| 7 | the store page | 2.1s | held 2.7s more → 4.8s |
| 8 | the list, before + | 3.5s | held 1.3s more → 4.8s |
| 9 | the new-requirement form | 7.3s | over its 5.4s minimum |
| 10 | the requirement details form | 28.9s | over its 7.4s minimum |
| 11 | the saved item | 3.9s | held 0.6s more → 4.5s |
| 12 | the login screen, at the end | 3.7s | held 0.5s more → 4.2s |

**Frame checks:**
- t=60s: yellow 3px ring, no fill/wash, rounded — on the "Select questions"
  field on the requirement details form. Confirms standard rule 3.
- Late-frame stuck-ring check: t=85s, t=90s, t=92s all clean — no lingering
  ring after logout (final email-verification/login screen shows only its own
  native blue focus outline).
