# add-question — ski-demo

**Purpose:** Video seed (not bug repro) — raw footage for a help/promo video,
finished in the Video-Editor repo.

**Standard:** 2026-09-09 raw-video standard (yellow-ring-only highlight held
through typing, per-screen narration lines already written into the vtt/script
beside this file, SCREEN_MIN_MS enforced per screen).

**Command used:**
```
cd Master_Flows/Recorder
CLICK_HIGHLIGHT_FILL=0 TYPE_HIGHLIGHT=1 \
SCREEN_MIN_MS="6200,6200,6500,5100,7100,9100,8600,3900,5100,3300,4200" \
RECIPE=add-question npx tsx scripts/record_flow.ts --surface bcp ski-demo add-question
```

**Target:** localhost (dev), surface: bcp.

**Result:** ski-demo_add-question_dev_09-09-26_v2.mp4 — 280,302,186 bytes
(267.3 MB), 93.28s. Flow passed. Expected window ~80-90s; this ran ~93.3s,
about 3s over — within reasonable margin given browser-launch overhead before
the flow's first step, not a defect.

**Deep links:** grep -c "deep link" on the flow's own log = 0 (Note: create_questions.test.ts
does not write a testing/log_reports/ entry at all — unlike create_items/create_collections,
it never calls logReportDirForStoreFile. The deep-link check above was run against the
Recorder's own full-output log, Master_Flows/Recorder/_logs/ski-demo/09-09_02-record-add-question.log,
which captures everything the flow printed. This is a gap in the create_questions/create_requirement
flows themselves, not something fixed here — flagged for whoever owns those flows.)

**bcp_runner browser:** "one window for all 1 step(s)" — single browser, no desktop seam.

**Screen table** (from the terminal's "⏱ screen N" lines — not present in saved log files):

| # | screen | on for | note |
|---|---|---|---|
| 1 | the sign-in screen | 5.9s | held 0.3s more → 6.2s |
| 2 | the code screen | 3.1s | held 3.1s more → 6.2s |
| 3 | dashboard | 1.1s | held 5.4s more → 6.5s |
| 4 | the list, before + | 1.4s | held 3.7s more → 5.1s |
| 5 | the new-question form | 11.2s | over its 7.1s minimum |
| 6 | the question details form | 25.6s | over its 9.1s minimum |
| 7 | dashboard | 6.1s | held 2.5s more → 8.6s |
| 8 | lookup, before picking the question | 6.1s | over its 3.9s minimum |
| 9 | the question, before activating | 6.9s | over its 5.1s minimum |
| 10 | the saved item | 3.3s | over its 3.3s minimum |
| 11 | the login screen, at the end | 3.7s | held 0.5s more → 4.2s |

**Frame checks:**
- t=35s: yellow 3px ring, no fill/wash, rounded — on the "Question Type" dropdown
  while the new-question form is on screen. Confirms standard rule 3.
- Late-frame stuck-ring check: t=85s and t=92s both clean — no lingering ring
  after logout (final email-verification/login screen at t≈92s shows only its
  own native blue focus outline, not a stray yellow ring).
